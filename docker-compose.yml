version: '3.8'
services:
  summit-dev-postgres:
    container_name: summit-dev-postgres
    image: postgres

    ports:
      - 5432:5432

    environment:
      - POSTGRES_USER=maidensix
      - POSTGRES_DB=summit_dev
      - POSTGRES_PASSWORD=M41d3nsix00
      - TZ=America/Sao_Paulo